package dynamicArrayImplementation;

public class Node {

	// build a node class
	// start with instance variables
	// move to constructor
	// finish with methods
	
	// variables - data being passed to the node
	// what is the node holding?
	int data;
	//Node node;
	
	// constructor - instantiate those variables
	public Node(int data) {
		this.data = data;
	}
	
	// methods - what do we want the node class to have inbuilt functionality for?
	// e.g. add, rm, size(), ...
	// these methods may not apply to node, but they will apply to 
		// array-based implementations of data structures such as dynamic arrays
	
}
